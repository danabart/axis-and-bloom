# Catalog Blueprint — brief series

Target architecture for coffees, archetypes, slots and the single write/read path. Reference docs: the **Catalog Blueprint** artifact (rev 1.1, 2026-09-13) and the earlier **Slot Truth Map** (rev 3, 2026-09-10). Decisions D1–D6 (Slot Truth Map) and N1–N7 (Blueprint) are settled; briefs implement them and do not re-open them.

Situation this series was written for: both roasteries deactivated, zero active coffees, repo at `745418a`. Because the catalog is empty, the plan is a **clean cut**: build the target model directly, move every reader in one reviewed brief, drop the old tables. No dual-write, no parity harness, no placement backfill.

Run in order. Each brief is deployable on its own and leaves the system working.

| # | Brief | Scope | Est. | Status |
|---|-------|-------|------|--------|
| 1 | `CLAUDE_CODE_PROMPT_CATALOG_1_SCHEMA_VIEWS_INTEGRITY.md` | `withTransaction()`; `archetype.code` as the one identity; `coffee_dial_slot` + `coffee_slot_assignment` with every constraint; `v_coffee_archetype`, `v_coffee`, `v_coffee_slot`, `v_coffee_sellable_slot`, `v_coffee_hop`; `catalogIntegrity.ts` at boot + `/admin/catalog/integrity` + admin panel + vitest. Additive only. | 2–3 d | EXECUTED 2026-09-13 · f4a9685 |
| 2 | `CLAUDE_CODE_PROMPT_CATALOG_2_SERVICE_ROUTES_IMPORT.md` | `catalogService.ts` (one function per verb, each in `withTransaction`, three-layer movement guardrail, returns integrity report); admin endpoints become thin wrappers; raw `POST /dial/positions` upsert + alias endpoints retired; roastery activate/deactivate moved in; `catalogImport.ts` with dry-run; seed files + in-schema Kopi Safari seed deleted. | 3–4 d | EXECUTED 2026-09-14, see `WHAT_WE_BUILT.md` #179 and `git log` (two real bugs found and fixed via the new tests: `setPriority`'s temp-priority CHECK violation, and integrity check 5's home-count double-count when a coffee has both a home and a guest row) |
| 3 | `CLAUDE_CODE_PROMPT_CATALOG_3_READERS_ONTO_VIEWS.md` | Every reader onto the views: resolver, Bloom endpoints, hops/legacy links, dial graph + suggestions, `getAliases()`, `fetchSommelierCoffees()` → `v_coffee_sellable_slot` (D2), Axis page / QR door / brew card / beats / cron → `v_coffee`; every enum→label map → `v_coffee_archetype`; Liam session snapshot keyed on `catalog_version`; CI greps for the three banned patterns. **The one careful review.** | 2–3 d | EXECUTED 2026-09-14 · `fc4efe8`, see `WHAT_WE_BUILT.md` #180 (`catalogReads.ts` new; `v_coffee_sellable_candidate` new; `v_archetype_adjacency` redefined on `v_coffee_hop`; `lint-catalog.mjs` — 4 rules, wired into CI and `npm test`; one discovered-not-in-Task-0 lint exception: `quizScoring.ts`/`quizIntegrity.ts`'s literal archetype labels, out of the quiz subsystem, allow-listed rather than rewritten) |
| 4 | `CLAUDE_CODE_PROMPT_CATALOG_4_ADMIN_PLACE_FLOW.md` | Coffees page: single "Place a coffee" form + blast-radius confirm; Dial page gestures call `moveCoffee` / `addGuest`; Inventory page SKU-only; slot spec editing; integrity page gains "placed, not yet sellable" + catalog changes feed from `api_event`. **Also removes `lint-catalog.mjs`'s `routes/admin.ts` allow-list** (rule 3, dormant legacy-table reads; rule 4, hardcoded labels) now that `admin.ts` itself moves onto the views. | 2–3 d | EXECUTED 2026-09-14 · `968d5ef`, see `WHAT_WE_BUILT.md` #181 (`AdminCoffees.tsx` deleted, replaced by `AdminCatalog.tsx`; `AdminDial.tsx` adapted in place via a `toGraph()` boundary transform, rendering untouched; 14 legacy admin GETs retired 410; `GET`/`PATCH /coffee-prices` deliberately *not* retired — a live, unrelated `coffee_retail_price` reader, Task 0 deviation; `routes/admin.ts`'s lint allow-list fully removed) |
| 5 | `CLAUDE_CODE_PROMPT_CATALOG_5_DROP_LEGACY.md` | Drop `dial_archetype_positions`, `coffee_alias`, `dial_slot_alias`, `dial_position_vocabulary`, `dial_archetype_config`, stored `hop_type`, `roaster_blend.archetype_id`, composite columns on `dial_slot_price` / `user_bloom_dial_current_position`; `coffees.roaster_id` + `roaster_blend.coffee_id` NOT NULL; legacy tables renamed to the `coffee_` convention; check 13 becomes a failure; WHAT_WE_BUILT_DB ownership table finalised. **Also**: remove the `coffee_alias` cascade from `catalogService.ts` (`deactivateRoastery`/`reactivateRoastery`'s last legacy write); re-key `dial_position_signal.suggested_vocabulary_id` → `suggested_slot_id` (`dialSuggestion.ts`'s last legacy read retires with it); drop `v_dial_positions`/`v_dial_navigation` (already zero TS readers as of brief 3); rename `v_archetype_adjacency` → `v_coffee_archetype_adjacency`; renames per the naming convention below; NOT NULLs; integrity check 13 becomes a failure (not info); `lint-catalog.mjs` allow-lists reduced to permanent entries only (the quiz-subsystem exception from brief 3). Then the first roastery enters via the import. | 1 d | **Stage A EXECUTED 2026-09-15 · `36db1f5`**, see `WHAT_WE_BUILT.md` #182 (all five tables + two views dropped; `hop_type`/`hop_type_enum`, `roaster_blend.archetype_id`, `coffees.roaster`, and the two `dial_slot_price`/`user_bloom_dial_current_position` composite columns dropped; four NOT NULLs added; `v_dial_position_consensus` → `v_coffee_dial_position_consensus`; pg_depend showed *fewer* real dependents than expected — `v_dial_navigation`/`v_dial_position_consensus` had none; three Task-0-missed live readers found and fixed: `admin.ts`/`dialSuggestion.ts`'s raw `hop_type` filters, `user_bloom_dial_current_position.dial_sort_order` on two customer-facing routes, `coffees.roaster` on `cron.ts`/`beatEngine.ts`/a dormant view; two deploy-time idempotency bugs found and fixed post-merge, `e0069ad`+`dc72678`). **Stage B EXECUTED 2026-09-16 · `1f10dc4`**, see `WHAT_WE_BUILT.md` #183 (the five tables + one view renamed to the `coffee_` convention; every index/constraint carrying an old table name renamed to match; two false positives from the rewrite script's `EXISTS` pattern matching an unrelated `ADD/DROP COLUMN IF EXISTS` statement caught and fixed; a full quiz run and a place-and-retire smoke-tested live on prod). |

Superseded by this series (do not execute): `features/coffee_assignment_integrity/CLAUDE_CODE_PROMPT_ASSIGNMENT_DOOR_PART1_ONE_WRITE_PATH.md` and `…PART2_IMPACT_AND_VISIBILITY.md` (2026-08-27). Absorbed: slot–instance Task A (spec + certification) from `features/slot_instance_model/SLOT_INSTANCE_STRATEGY_V1.md`.

**Naming convention (Dana, 2026-09-13):** every catalog table, enum and view starts with `coffee_` (views `v_coffee_…`) so the catalog groups together in SQL listings. New objects follow it from brief 1; surviving legacy tables renamed in brief 5b: `archetype` → `coffee_archetype`, `archetype_assignments` → `coffee_archetype_assignment`, `roaster_blend` → `coffee_sku`, `dial_coffee_relationships` → `coffee_hop`, `dial_slot_price` → `coffee_slot_price`, `v_archetype_adjacency` → `v_coffee_archetype_adjacency`. `coffees` itself stays `coffees`.

House rules that apply to every brief here: one writer per fact, via a named service function; derivations used by two or more readers are SQL views; constraints, not comments; integrity green is part of done; every brief updates the ownership table in `WHAT_WE_BUILT_DB.md`.

---

**Series complete on 2026-09-16.** Every catalog table and view now follows the `coffee_` naming convention; the seven legacy placement objects and every dead column they carried are gone; `catalogIntegrity.ts`'s 13 checks (10 failing-type + 3 informational) are the live source of truth at boot and via `/admin/catalog/integrity`. Catalog data enters via `npm run catalog:import` (dry-run capable, see `catalogImport.ts`) or the Place-a-coffee form only — the first real roastery's import is the next, un-scripted step, not part of this series.
