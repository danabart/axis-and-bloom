# This folder is CODE-ONLY

The launch/marketing plan that used to live here moved to **`/launch`** at the repo root
(reorganized into workstream folders on 2026-07-18; the original flat files are in
`launch/_archive/`).

Per the plan's standing rules, `backend/src/features/marketing/` is the home for new
**backend marketing code** (analytics event handlers, Mailchimp sync, reporting helpers,
feedback-loop logic, …) created by the numbered Claude Code steps. Routes stay thin and
import from here.

Start at `launch/README.md`.
